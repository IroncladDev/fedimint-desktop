procs:
  user:
    shell: zsh
    stop: SIGKILL
  dioxus:
    shell: dx serve
    stop: SIGTERM
  tailwind:
    shell: bunx tailwindcss -i ./src/input.css -o ./assets/tailwind.css --watch
    stop: SIGINT
